export const DEFAULT_SELECTED_INDEX = 9;
export const SEARCH_PATTERN = ".*{searchText}.*";
export const SEARCH_GROUP_PATTERN = /search:(\w+)\sgroup:(\w+)/;

//Normally api url must be in the env file but this project is a client project so I store url as a constant.
export const API_URL = "https://countries.trevorblades.com/graphql";
